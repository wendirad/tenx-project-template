version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/requirements"
    schedule:
      interval: "weekly"
    groups:
      python-dependencies:
        patterns:
          - "*" # Groups all pip packages into a single PR
